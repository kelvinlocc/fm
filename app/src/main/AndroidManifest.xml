<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="family_move.accordhk.com.familymove">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity_not_use"
            android:label="@string/app_name">

        </activity>


        <activity
            android:name=".splashScreenActivity"
            android:theme="@style/splashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginActivity"></activity>
        <activity android:name=".termsActivity"></activity>
        <activity android:name=".registerActivity"></activity>
        <activity android:name=".forgetPwActivity"></activity>
        <activity android:name=".mainActivity">

        </activity>
        <activity android:name=".setting.readTermsActivity"></activity>
        <activity android:name=".setting.thanksActivity"></activity>
        <activity android:name=".setting.accountChangePwActivity"></activity>
        <activity android:name=".special_activity.youtube_activity"></activity>
    </application>

</manifest>